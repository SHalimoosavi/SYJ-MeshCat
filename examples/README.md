# Examples

This directory will hold runnable examples once the corresponding
milestones land:

- `examples/android/` — sample integrations against the app's internal
  APIs (added around Milestone 10).
- `examples/python/` — Python SDK usage scripts, e.g. scripted mesh
  send/receive, statistics polling (added around Milestone 9).

Currently empty — this is a Milestone 1 placeholder.
